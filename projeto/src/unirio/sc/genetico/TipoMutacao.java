package unirio.sc.genetico;

public enum TipoMutacao {
	UNIFORM_MUTATION,
	NON_UNIFORM_MUTATION,
	MOVE_GNE,
	MOVE_JOIN_SPLIT,
	MOVE_JOIN_SPLIT_V02,
	MOVE_GGA,
	JOIN,
	JOIN_V02,
	NENHUMA
}
